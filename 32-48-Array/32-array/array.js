// array = type of a variable which stores multiple values

let fruits = ["apple", "orange", "grapes"];

//fruits[0] = "coconut";

fruits.push("lemon"); //add an element
fruits.pop(); //removes last element
fruits.unshift("sitafal"); //add element to beginning
fruits.shift(); // removes last element

let length = fruits.length;
let index = fruits.indexOf("mango");

console.log(index);
